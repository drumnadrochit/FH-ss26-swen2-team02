info:
  name: Delete Tour
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{host}}/api/tours/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
