info:
  name: Export Tours
  type: http
  seq: 6

http:
  method: GET
  url: "{{host}}/api/tours/export"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
