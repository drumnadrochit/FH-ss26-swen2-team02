info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/api/auth/login"
  body:
    type: json
    data: |-
      {
        "Username":"Vukbo",
        "Password":"2000"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setVar("token", res.body);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
